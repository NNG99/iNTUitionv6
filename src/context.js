import React, { Component } from 'react';

const BankContext = React.createContext();

class BankProvider extends Component {
    state = {  }
    render() { 
        return (  );
    }
}
 
export default BankProvider;